name: nabiz

on:
  schedule:
    - cron: "0 20 * * *"
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: hatirlatma
        run: |
          echo "Bu runner grok-4.5 değil. Nabız sohbet tarafında (web_search + x_search)."
          echo "XAI_API_KEY yoksa olgu yazma. C: yok."
          test -f NABIZ.md
          test -f .github/skills/karicim-nabiz/SKILL.md

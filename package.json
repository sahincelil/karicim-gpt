{
  "name": "karicim-gpt",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check api/chat.js && node --check api/agent.js && node --check lib/security.js && node --check app.js && node --check server.js"
  }
}

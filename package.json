{
  "name": "karicim-gpt",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check api/chat.js && node --check api/agent.js && node --check lib/security.js && node --check app.js"
  }
}
